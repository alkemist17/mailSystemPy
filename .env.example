# Archivo de ejemplo de variables de entorno
# Copia este archivo a .env y completa con tus valores reales
# IMPORTANTE: Nunca subas el archivo .env al control de versiones

# ============================================================================
# CONFIGURACIÓN SMTP
# ============================================================================

# Servidor SMTP (ejemplos comunes):
# Gmail: smtp.gmail.com
# Outlook/Hotmail: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
# Servidor personalizado: smtp.tudominio.com
SMTP_SERVER=smtp.gmail.com

# Puerto SMTP
# 587: Puerto estándar para TLS (recomendado)
# 465: Puerto para SSL
# 25: Puerto sin encriptación (no recomendado)
SMTP_PORT=587

# Usuario SMTP (generalmente tu dirección de correo)
SMTP_USER=tu_email@gmail.com

# Contraseña SMTP
# Para Gmail, necesitas usar una "Contraseña de aplicación" en lugar de tu contraseña normal
# Puedes generarla en: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=tu_contraseña_o_app_password

# Dirección de correo que aparecerá como remitente
# Generalmente es la misma que SMTP_USER
SMTP_FROM_EMAIL=tu_email@gmail.com

# Usar TLS para encriptar la conexión (true/false)
# Recomendado: true para mayor seguridad
SMTP_USE_TLS=true

# ============================================================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================================================

# API Key para autenticación
# Genera una clave segura y única. Ejemplo: openssl rand -hex 32
# Los clientes deben enviar esta clave en el header X-API-Key
API_KEY=tu_api_key_secreta_aqui

# IPs permitidas (whitelist)
# Separa múltiples IPs con comas
# Soporta IPs individuales: "127.0.0.1,192.168.1.100"
# Soporta rangos CIDR: "192.168.1.0/24"
# Ejemplo para servidor local y remoto: "127.0.0.1,203.0.113.45"
ALLOWED_IPS=127.0.0.1
